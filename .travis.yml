language: node_js
node_js:
  - "4.2"

install:
  - npm install -g typescript
  - npm install -g ts-node
  - npm install -g gulp-cli

deploy:
  provider: azure_web_apps
  username: "pbptest1"
  password: "Shayne1234"
  site: "pbptest1"
  verbose: true